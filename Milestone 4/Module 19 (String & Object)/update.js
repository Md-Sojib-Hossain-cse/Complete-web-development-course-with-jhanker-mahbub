let person  = {
    name : 'sojib',
    age : 19,
}

//store keyname as a string in a variable and update value using the variable
let userName = 'name';
person[userName] = 'Ariful';
console.log(person);