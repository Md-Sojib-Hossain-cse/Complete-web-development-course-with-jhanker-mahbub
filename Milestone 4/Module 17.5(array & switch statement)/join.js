// Join method returns an array as a string 
//join method does not change the original array
//any seperator can be specified default is comma (,)

// syntax : arrayName.join("seperator")

const food = ['apple','orange','banana','jackfruit'];

console.log(food.join('+'));
console.log(food.join('|'));
console.log(food.join('-'));