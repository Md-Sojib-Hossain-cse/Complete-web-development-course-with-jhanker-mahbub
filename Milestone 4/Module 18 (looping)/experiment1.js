let departments = ['cmt','mt','ent','ch','rac'];
let department;

for (department of departments){
    if(department === 'ent'){
        console.log(department , "is available");
        break;
    }
}